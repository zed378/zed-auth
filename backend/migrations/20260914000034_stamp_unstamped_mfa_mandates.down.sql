-- Nothing. Removing the stamps would put those organizations back into a grace
-- period that never ends — restoring the bug, not the schema. The key is
-- ignored by a build that predates P3-07, so leaving it costs nothing.
SELECT 1;
