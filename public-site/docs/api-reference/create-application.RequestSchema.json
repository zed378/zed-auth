{"title":"Body","body":{"required":true,"content":{"application/json":{"schema":{"type":"object","description":"There is no `project_id` or `org_id` here — both come from the path.\n","required":["name","type"],"additionalProperties":false,"properties":{"name":{"type":"string","minLength":1,"maxLength":200},"type":{"type":"string","enum":["web","native","spa","api","saml"],"description":"Decides whether the client can hold a secret. `spa` and `native` are\npublic — they cannot keep one confidential in a browser or a shipped\nbinary, so they use PKCE and are refused a secret at the database\nlevel.\n\n**Immutable after creation.** Changing it would either strand a secret\non a now-public client or leave a confidential one with none.\n","title":"ApplicationType"},"saml":{"allOf":[{"type":"object","description":"A SAML registration as supplied by a caller.\n\nEither `metadata_xml` **or** the fields, not both. Metadata is the\nnormal path — the other party publishes a document and typing its\ncontents out by hand is how an entity ID acquires a trailing space —\nand the manual fields are there for a service provider that publishes\nnone.\n","additionalProperties":false,"properties":{"metadata_xml":{"type":"string","maxLength":524288,"description":"The service provider's SAML metadata document.\n\nParsed under the same hardening as an assertion: bounded in size\nand nesting depth, DTDs and entity references refused, and a\ndocument that does not survive a parse and re-serialise unchanged\nrejected before anything reads it. It is untrusted XML from an\nexternal party and is treated as such.\n\n`entity_id`, `acs_url`, `certificate` and `want_signed_requests`\nare read from it. Where it advertises several Assertion Consumer\nServices, the HTTPS HTTP-POST one wins, `isDefault` before the\nlowest `index`.\n"},"entity_id":{"type":"string","maxLength":1024},"acs_url":{"type":"string","format":"uri","maxLength":2048},"attribute_release":{"type":"array","items":{"type":"string","enum":["display_name","email","role_keys","username"],"description":"An attribute a SAML assertion can carry.\n\nA closed set, and a name outside it is refused rather than stored: an\nattribute nobody produces is a setting that saves cleanly and releases\nnothing, and the administrator who chose it would believe the service\nprovider receives something it does not.\n","title":"SamlAttribute"}},"want_signed_requests":{"type":"boolean"},"certificate":{"type":"string"},"allow_idp_initiated":{"type":"boolean"}},"title":"SamlRegistrationInput"}],"description":"Required when `type` is `saml`, and refused otherwise.\n\nRequired because a `saml` application without one is a registration\nnothing can use, and creating it in a second call would leave a\nwindow in which the application exists and no `AuthnRequest` can be\nmatched to it.\n"},"redirect_uris":{"type":"array","items":{"type":"string","format":"uri"}},"post_logout_redirect_uris":{"type":"array","items":{"type":"string","format":"uri"}},"allowed_origins":{"type":"array","items":{"type":"string"},"description":"Browser origins permitted to read responses obtained with this\napplication's tokens. Empty by default, which means none — see\n`Application.allowed_origins`.\n","example":["https://app.example.com"]},"grant_types":{"type":"array","items":{"type":"string"},"description":"Defaults to `[\"authorization_code\", \"refresh_token\"]`. `implicit`\nand `password` are refused for every type: both hand credentials or\ntokens to places that cannot protect them, and OAuth 2.1 removes\nthem.\n"}},"title":"ApplicationCreate"}}}}}