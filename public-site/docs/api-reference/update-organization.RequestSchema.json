{"title":"Body","body":{"required":true,"content":{"application/json":{"schema":{"type":"object","description":"A partial update. An absent property is left alone; `domain: null`\nreleases the domain.\n","additionalProperties":false,"properties":{"name":{"type":"string","minLength":1,"maxLength":200},"domain":{"type":"string","nullable":true,"maxLength":253},"status":{"type":"string","enum":["active","suspended"],"description":"Requires `INSTANCE_OWNER`."},"settings":{"type":"object","description":"The organization's policy (`docs/PLAN/08` Part B).\n\n**Unknown properties are rejected rather than stored.** A misspelled\nkey kept silently becomes a policy that is not in force and looks like\nit is, and nobody notices until the audit that was supposed to find it\ndoes not.\n\nEvery `default` here is the value the service applies when an\norganization's settings say nothing — the same value the\n`organizations.settings` column default carries and the same one\n`authn.DefaultPolicy` and `authn.DefaultLoginPolicy` hold. They are\ndocumented here because the console must show an administrator what\nthey are changing *from*, and a console restating them would be a third\ncopy free to drift. `TestSpecDefaultsMatchTheService` fails if the two\nGo values and these stop agreeing.\n","additionalProperties":false,"properties":{"password_policy":{"type":"object","additionalProperties":false,"properties":{"min_length":{"type":"integer","minimum":12,"maximum":128,"default":12,"description":"At least the platform minimum. An organization policy may raise\nit and never lower it — a tenant setting that can go below the\ninstance floor is a per-tenant way to disable a platform\ncontrol.\n"},"require_uppercase":{"type":"boolean","default":true},"max_age_days":{"type":"integer","minimum":0,"maximum":3650,"default":90,"description":"`0` means passwords never expire, which is a real choice rather\nthan an absent value: NIST SP 800-63B argues forced rotation\nmakes passwords worse.\n"}}},"mfa_required":{"type":"boolean","default":false,"description":"Stored and validated; **not enforced**. Multi-factor enrolment\narrives in Phase 3, and until it does this records an intention\nrather than a control. No surface may describe it as active\n(`docs/UI-UX/21` governance rule).\n"},"session_lifetime_hours":{"type":"integer","minimum":1,"maximum":720,"default":12},"allowed_login_methods":{"type":"array","minItems":1,"default":["password"],"items":{"type":"string","enum":["password"]},"description":"Only `password` is available in this phase. `passkey` and `social`\nare planned and are rejected until they work — an API that accepts\na method nothing implements would silently disable every method\nthat does.\n"}},"title":"OrganizationSettings"}},"title":"OrganizationUpdate"}}}}}